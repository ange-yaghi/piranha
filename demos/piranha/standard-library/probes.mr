private import "literals.mr"
private import "conversions.mr"

public node throw_runtime_error => __piranha__throw_runtime_error {
    input throw [bool]: false;
}

public node int_probe => __piranha__int_probe {
    input __in [int];
}
