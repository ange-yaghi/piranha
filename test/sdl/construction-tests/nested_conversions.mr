public node float => __piranha__float {
    input __in: 10.0;
}

public node string => __piranha__string {
    input __in: "DEFAULT";
}

float top_level(
    string(
        float(
            string(
                string(
                    float(
                        float(
                            float(
                                string("10.5")
                            )
                        )
                    )
                )
            )
        )
    )
)
